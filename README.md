# LINE_APP_SYSTEM

Repository containing multiple LINE-related projects:

- `apps/esp32-line-app` - ESP32 firmware project
- `apps/test-send-msg` - Python scripts to send messages
- `apps/line-dashboard` - FastAPI dashboard to send LINE Notify messages

See each app's own README for details on running and configuration.
